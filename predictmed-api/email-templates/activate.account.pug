extends template
block content
    div.content
        h1.title Hello #{userName}!
        br
        | Please verify your account,
        | just click on the link
        br
        a(href=`http://localhost:${port}/auth/activate/mail/${emailToken}`) Verify