import {createTheme} from "@mui/material/styles";

export const styles = createTheme({
    chatWindow: {
        minWidth: 650,
    },
    chatWindowMessages: {
        width: '100%',
        height: '80vh'
    }
})