import pages from './pages';
import home from './home';
import prediction from './prediction';
import support from './support';

const menuItems = {
  items: [home, pages, prediction, support]
};

export default menuItems;
